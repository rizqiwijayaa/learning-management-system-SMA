Future<String> downloadBase64File({
  required String fileName,
  required String base64Data,
  required String mimeType,
}) async {
  throw UnsupportedError(
    'Download file belum didukung di platform ini.',
  );
}
