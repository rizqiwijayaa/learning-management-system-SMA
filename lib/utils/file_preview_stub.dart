Future<void> previewBase64File({
  required String fileName,
  required String base64Data,
  required String mimeType,
}) async {
  throw UnsupportedError('Preview file belum didukung di platform ini.');
}
